# 事故報告書作成AIエージェント — タスク管理

> 要件定義書: `requirements.md`  
> 技術スタック: Next.js 16 / TypeScript / Tailwind CSS  
> プロトタイプ方針: モックデータでAI処理を再現。Azure連携・認証はスコープ外。

---

## 1. 基盤構築

### 1.1 プロジェクトセットアップ
- [x] Next.js 16 + TypeScript + Tailwind CSS の初期構成確認
- [x] pnpm ワークスペース設定
- [x] `app/globals.css` を日本語フォント対応に更新（Noto Sans JP）
- [x] `app/layout.tsx` にヘッダーナビ・メタデータを設定
- [x] `public/mock/` にモック画像を配置（天井ボード落下 × 2、エスカレーター × 2）

### 1.2 型定義
- [x] `lib/types.ts` — `Report`, `Victim`, `FiveWTwoH`, `Photo`, `Feedback`, `CreateReportInput`, `ProcessingStep` を定義

---

## 2. モックサービス層（`lib/`）

> 将来は Azure OpenAI / Azure AI Vision / Microsoft Foundry Agent Service に差し替え予定。関数境界のみ固定。

- [x] `lib/mock-camera.ts` — キーワード判定でシナリオ（天井 / エスカレーター / その他）を検出し、モック画像を返す
  - 天井 (`天井`, `天板`, `ボード`) → `ceiling-board-fall-*.png`
  - エスカレーター (`エスカレーター`) → `escalator-acrylic-fall-*.png`
  - その他 → 画像なし（空配列）
- [x] `lib/mock-vision.ts` — 画像URLをもとに固定観察文を返す
- [x] `lib/mock-agent.ts` — カメラ取得 → 画像解析 → テンプレート生成を順次実行
- [x] `lib/report-template.ts` — シナリオ別に5W2H・原因・処置・防止対策・本文を生成
  - JST（Asia/Tokyo）でのタイムゾーン対応済み
  - 天井ボードシナリオ / エスカレーターシナリオ / 汎用シナリオの3テンプレート
- [x] `lib/diff.ts` — PATCHリクエスト時にAI初期出力との差分を計算し `Feedback` 配列を生成
- [x] `lib/report-store.ts` — インメモリストア（Map）
  - 時刻ベースのステップシミュレーション（700ms × 7ステップ ≒ 5秒）
  - `getReport()` および `getProcessingSteps()` の両方でステータス自動遷移

---

## 3. API ルート（`app/api/`）

| メソッド | パス | 実装 | 内容 |
|---|---|---|---|
| POST | `/api/reports` | [x] | 報告書作成・ドラフト生成 |
| GET | `/api/reports` | [x] | 一覧取得（降順） |
| GET | `/api/reports/[id]` | [x] | 報告書取得（ステータス自動遷移） |
| PATCH | `/api/reports/[id]` | [x] | 修正保存・差分記録 |
| GET | `/api/reports/[id]/status` | [x] | 処理ステップ状況ポーリング |
| POST | `/api/reports/[id]/confirm` | [x] | 確定処理 |

---

## 4. 画面実装

### 4.1 新規事故報告書作成画面（`/`）
- [x] 入力フォーム実装（事故概要・発生日時・発生場所・補足情報・被害者有無・復旧状況・金額影響）
- [x] デモ用初期値を入力済み状態で表示
- [x] バリデーション（必須項目チェック）
- [x] 送信後に `/reports/[id]` へリダイレクト

### 4.2 作成中画面（`/reports/[id]` — 処理中状態）
- [x] グラスモーフィズムUI（blue-indigo グラデーション＋backdrop-blur）でAIらしさを表現
- [x] 7ステップをアイコン付きで表示（完了 / 進行中 / 未着手）
- [x] ポーリング（600ms間隔）でステップをリアルタイム更新
- [x] 完了後に自動でページリロード → レポート編集画面へ遷移

### 4.3 ドラフト確認・編集画面（`/reports/[id]`）
- [x] 「事故報告書」タブ
  - [x] 基本情報（概要・発生日時・場所・報告日・報告者・金額）
  - [x] 被害者情報（有無に応じて表示切替）
  - [x] 5W2H（全項目インライン編集可）
  - [x] 原因・処置・防止対策（テキストエリア編集）
  - [x] 報告書本文（テキストエリア編集）
- [x] 「写真台帳」タブ
  - [x] 最大8枚の2列グリッド表示（番号①〜⑧）
  - [x] 写真場所名称のインライン編集
  - [x] 写真の上下移動（順序変更）
  - [x] 写真の削除
  - [ ] 写真の追加（プロトタイプではモック画像選択UIが未実装）
- [x] 「AI出力と修正差分」タブ（件数バッジ表示）
- [x] 修正保存ボタン（保存完了後にフィードバックトースト表示）
- [x] 確定ボタン（確認ダイアログ付き）
- [x] 確定済みの場合は編集不可（読み取り専用）
- [x] AIドラフトである旨のインフォバナー表示
- [x] 帳票プレビューリンク

### 4.4 帳票プレビュー画面（`/reports/[id]/preview`）
- [x] 1ページ目：基本情報・被害者情報・5W2H・原因・処置・防止対策・本文
- [x] 2ページ目：写真台帳（最大8枚・番号・場所名・カメラ名・撮影日時）
- [x] 印刷 / PDF保存ボタン（`window.print()`）— `PrintButton.tsx` として Client Component に分離（`page.tsx` は Server Component を維持）

### 4.5 差分確認画面（`/reports/[id]/diff`）
- [x] AI出力（赤）→ 修正後（緑）→ 差分サマリー（青）の3段表示
- [x] 修正履歴が0件の場合は案内メッセージ表示
- [x] AI改善フィードバックメッセージ表示

### 4.6 事故報告書一覧画面（`/reports`）
- [x] テーブル形式（ID・概要・場所・発生日時・ステータス・作成日時）
- [x] ステータスバッジ（処理中 / 確認中 / 修正済 / 確定済）カラー付き
- [x] 報告書がない場合のエンプティステート表示

---

## 5. 受け入れ条件チェック（`requirements.md § 18`）

| 条件 | 状態 |
|---|---|
| 事故概要、日時、場所を入力できる | [x] |
| AIエージェントへの作成依頼ができる | [x] |
| 作成中ステップが表示される | [x] |
| 事故報告書ドラフトが表示される | [x] |
| 1ページ目の事故報告書本体を確認できる | [x] |
| 2ページ目の写真台帳を確認できる | [x] |
| 最大8枚の写真枠を表示できる | [x] |
| 写真場所名称を編集できる | [x] |
| 5W2Hを編集できる | [x] |
| 原因、処置、防止対策を編集できる | [x] |
| 報告書本文を編集できる | [x] |
| 修正内容を保存できる | [x] |
| AI出力と修正後の差分を確認できる | [x] |
| 確定操作ができる | [x] |
| 一覧画面で確定済み報告書を確認できる | [x] |

---

## 6. 残タスク・改善候補

### 優先度：高（デモ品質向上）
- [ ] **写真追加UI** — 「プロトタイプではモック画像を選択できる」仕様を実装（モック画像一覧からピッカー）
- [ ] **処理中画面の独立URL** — `/reports/[id]` 内で status に応じて切り替えているが、ページ遷移時のちらつき改善（Suspense / skeleton）
- [ ] **スケルトンローディング** — 報告書取得中のレイアウトシフト防止（requirements.md § 15）
- [ ] **エスカレーターシナリオのデモ確認** — 実際に入力してカメラ画像・報告書内容が切り替わることを確認

### 対応済み
- [x] **特定部署名の除去** — `施設管理部` / `施設管理担当者` をアプリコード全体から削除。`app/layout.tsx`・`lib/mock-agent.ts`・`lib/report-template.ts`・`app/reports/[id]/preview/page.tsx` を修正（社内全体利用を想定した汎用表記に統一）

### 優先度：中（品質・UX）
- [ ] **被害者情報の編集** — 現状は読み取り専用。被害者区分・性別・年齢・被害程度の編集UIを追加
- [ ] **復旧日時の編集** — `recoveredAt` フィールドの入力UI
- [ ] **報告書タイトルの編集** — 現状固定値「事故報告書」
- [ ] **差分タブのナビゲーション** — `/reports/[id]/diff` への遷移ボタンを編集画面に追加（現在はタブ内表示のみ）
- [ ] **一覧画面でのページネーション** — 件数が増えた場合の対応

### 優先度：低（将来対応）
- [ ] **PDF出力** — `window.print()` で代替中。react-pdf 等のライブラリ導入を検討
- [ ] **永続化** — 現状はインメモリ。サーバー再起動でリセット。開発確認用の `reports.json` へのファイル書き出しなど
- [ ] **エラーバウンダリ** — ページレベルの `error.tsx` 実装

---

## 7. 将来拡張（スコープ外・要件定義 § 20）

- [ ] Microsoft Foundry Agent Service 連携（`lib/mock-agent.ts` を差し替え）
- [ ] Azure OpenAI による報告書生成（`lib/report-template.ts` を差し替え）
- [ ] Azure AI Vision による画像解析（`lib/mock-vision.ts` を差し替え）
- [ ] Azure Blob Storage への画像保存
- [ ] Cosmos DB への報告書・修正履歴保存（`lib/report-store.ts` を差し替え）
- [ ] 社内カメラ画像取得API連携（`lib/mock-camera.ts` を差し替え）
- [ ] Microsoft Entra ID 認証
- [ ] Teams通知
- [ ] PDF / Word 完全生成
- [ ] 帳票テンプレート管理画面
- [ ] プロンプト改善管理画面（修正差分データの活用）

---

## 8. デモシナリオ確認（`requirements.md § 19`）

| # | ステップ | 対応画面 | 確認 |
|---|---|---|---|
| 1 | 現場担当者がWebアプリを開く | `/` | [x] |
| 2 | 事故概要、発生日時、発生場所を入力する | `/` | [x] |
| 3 | 「AIエージェントに作成を依頼」を押す | `/` → `/reports/[id]` | [x] |
| 4 | AIがカメラ画像を取得・解析しているように表示する | `/reports/[id]`（processing） | [x] |
| 5 | 天井ボード落下の画像付き事故報告書ドラフトが生成される | `/reports/[id]`（review） | [x] |
| 6 | 1ページ目の事故報告書本体を確認する | 「事故報告書」タブ or プレビュー | [x] |
| 7 | 2ページ目の写真台帳を確認する | 「写真台帳」タブ or プレビュー | [x] |
| 8 | 写真場所名称を修正する | 「写真台帳」タブ | [x] |
| 9 | 原因や防止対策を一部修正する | 「事故報告書」タブ | [x] |
| 10 | 修正内容を保存する | 「修正内容を保存」ボタン | [x] |
| 11 | AI出力との差分を確認する | 「AI出力と修正差分」タブ | [x] |
| 12 | 「この内容で確定」を押す | 「この内容で確定」ボタン | [x] |
| 13 | 確定済み事故報告書として一覧に表示される | `/reports` | [x] |
